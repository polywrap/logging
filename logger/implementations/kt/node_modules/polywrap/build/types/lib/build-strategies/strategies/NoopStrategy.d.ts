import { BuildStrategy } from "../BuildStrategy";
export declare class NoopBuildStrategy extends BuildStrategy<void> {
    getStrategyName(): string;
    buildSources(): Promise<void>;
    build(): Promise<void>;
}

import { Logger } from "../../logging";
export declare enum UrlFormat {
    git = ".git"
}
export declare function parseUrlFormat(url: string): UrlFormat;
export declare const downloadProjectTemplate: (url: string, projectDir: string, logger: Logger, urlFormat?: UrlFormat) => Promise<void>;

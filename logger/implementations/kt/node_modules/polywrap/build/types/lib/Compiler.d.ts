import { PolywrapProject, PluginProject, SchemaComposer } from "./";
import { BuildStrategy } from "./build-strategies/BuildStrategy";
export interface CompilerConfig {
    outputDir: string;
    project: PolywrapProject | PluginProject;
    buildStrategy?: BuildStrategy;
    schemaComposer: SchemaComposer;
}
export declare class Compiler {
    private _config;
    constructor(_config: CompilerConfig);
    compile(): Promise<boolean>;
    private _isWasm;
    private _isInterface;
    private _buildModules;
    private _outputWrapManifest;
    private _copyResourcesFolder;
    private _maybeAssembleDocsDir;
    private _validateWasmModule;
}

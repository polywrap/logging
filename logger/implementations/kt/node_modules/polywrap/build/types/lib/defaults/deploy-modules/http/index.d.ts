import { DeployModule } from "../../../deploy";
import { Uri } from "@polywrap/core-js";
declare class HTTPDeployer implements DeployModule {
    execute(uri: Uri, config?: {
        postUrl: string;
        headers: {
            name: string;
            value: string;
        }[];
    }): Promise<Uri>;
}
declare const _default: HTTPDeployer;
export default _default;

import { Logger } from "../logging";
import { WrapManifest, WrapAbi } from "@polywrap/wrap-manifest-types-js";
export declare const supportedWrapTypes: WrapManifest["type"][];
export declare const generateWrapFile: (abi: WrapAbi, name: string, type: string, path: string, logger: Logger) => Promise<void>;

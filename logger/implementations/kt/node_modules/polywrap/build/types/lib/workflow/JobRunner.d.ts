import { JobResult } from "./types";
import { ClientConfigBuilder } from "@polywrap/client-js";
import { MaybeAsync } from "@polywrap/core-js";
import { WorkflowJobs } from "@polywrap/polywrap-manifest-types-js";
export declare class JobRunner {
    private _configBuilder;
    private _onExecution?;
    private _jobOutput;
    private _client;
    constructor(_configBuilder: ClientConfigBuilder, _onExecution?: ((id: string, JobResult: JobResult<unknown>) => MaybeAsync<void>) | undefined);
    run(jobs: WorkflowJobs, ids: string[]): Promise<void>;
    private _getJobId;
    private _followAccessors;
    private _resolveReference;
    private _resolveRecord;
    private _resolveArray;
    private _resolveValue;
    private _execStep;
    private _executeSteps;
    private _isReference;
    private _isRecord;
}

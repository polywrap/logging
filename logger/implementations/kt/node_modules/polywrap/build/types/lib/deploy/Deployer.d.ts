import { DeployJobResult } from ".";
import { CacheDirectory, Logger } from "..";
import { DeployManifest } from "@polywrap/polywrap-manifest-types-js";
export declare class Deployer {
    static cacheLayout: {
        root: string;
        deployModulesDir: string;
    };
    manifest: DeployManifest;
    private _config;
    private constructor();
    static create(manifest: string, logger: Logger): Promise<Deployer>;
    run(): Promise<DeployJobResult[]>;
    getCacheDir(): CacheDirectory;
    private _sanitizePackages;
    private _getDeployModule;
    private _cacheDeployModules;
    private _validateManifestWithExts;
}

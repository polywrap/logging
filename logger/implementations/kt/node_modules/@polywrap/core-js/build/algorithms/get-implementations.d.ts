import { Uri, CoreClient, WrapError, ReadonlyUriMap } from "../types";
import { IUriResolutionContext } from "../uri-resolution";
import { Result } from "@polywrap/result";
export declare const getImplementations: (wrapperInterfaceUri: Uri, interfaces: ReadonlyUriMap<readonly Uri[]>, client?: CoreClient, resolutionContext?: IUriResolutionContext) => Promise<Result<Uri[], WrapError>>;
